# Test Plan
Generated by /plan-eng-review on 2026-04-09
Branch: fix/chat-quality-improvements
Repo: ianrang/essenly-proj

## Affected Pages/Routes
- `/api/chat` — Full chat flow with personalization (eval harness target)
- `/api/profile/onboarding` — New onboarding endpoint (profile save)
- Chat page — OnboardingChips component, ProductCard compact, card rendering

## Key Interactions to Verify
- OnboardingChips: skin type selection → concerns selection → "Start chatting" on chat page
- OnboardingChips: "Skip" flow → SuggestedQuestions on chat page
- ProductCard compact: "Get free kit" button for is_highlighted products on chat page
- ProductCard compact: "Buy Online" button for regular products on chat page
- Profile-based search filtering: saved profile changes search results via /api/chat

## Edge Cases
- OnboardingChips: no skin type selected + Start click → should show validation error
- OnboardingChips: 3+ concerns selected → should be prevented by UI
- OnboardingChips: API error on profile save → should show error, not crash
- Kit CTA: is_highlighted product in scroll group alongside regular products
- Empty profile: search should return unfiltered results
- VP-1: is_highlighted must not affect search scoring or ranking

## Critical Paths
- Onboarding → profile save → first chat message → personalized recommendation (end-to-end)
- Search with profile filters → correct product filtering → card rendering
- Guardrails: medical advice rejection, off-topic redirect, adversarial defense
- Multilingual: Japanese, Chinese, Thai conversation quality
