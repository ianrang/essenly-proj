import { NextResponse } from "next/server";

// TODO: Implement — TDD §4.2
// POST /api/chat — 스트리밍 대화 (SSE)
export async function POST() {
  return NextResponse.json({ error: "Not implemented" }, { status: 501 });
}
