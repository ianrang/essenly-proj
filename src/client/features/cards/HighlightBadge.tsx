"use client";

import "client-only";

// TODO: Implement HighlightBadge — PRD VP-1
// - Renders badge for is_highlighted items
// - Display ONLY — never affects search/sort/filter (Q-2)
export default function HighlightBadge() {
  return <div>HighlightBadge — TODO</div>;
}
