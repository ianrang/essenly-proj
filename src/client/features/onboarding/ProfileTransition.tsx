"use client";

import "client-only";

// TODO: Implement profile generation transition — TDD §3.3
// - Loading animation while DV-4 generates
// - Transition to profile confirmation
export default function ProfileTransition() {
  return <div>ProfileTransition — TODO</div>;
}
