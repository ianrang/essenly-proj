import "server-only";

// TODO: Implement treatment domain logic — TDD §3.3 + §3.5 F1
// - Treatment filtering (downtime safety check, etc.)
// - Imports: ./judgment.ts (one-way only, §2.3)
// Pure function — no DB/API calls (R-7, L-7)
