import "server-only";

// TODO: Implement Chat orchestrator — TDD §4.2
// - Load history + profile → build system prompt → call LLM → stream response
// - Imports: core/ai-engine, core/memory, ./prompts, ./tools/
